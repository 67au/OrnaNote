--- old
+++ new
@@ -12,1 +12,1 @@
-只能优化连接的延时（特别是用**中国移动**的玩家），而关于游戏数据库操作的延时不会降低，例如背包相关的，因为这是游戏的问题，所以这种情况应是**清理背包**。
+只能优化连接的延时（特别是用**中国移动**的玩家），而关于游戏数据库操作的延时不会降低，特别是背包相关的，因为这是游戏的问题，所以这种情况应是**清理背包**。
@@ -14,2 +14,2 @@
-1. 第一种方法：使用加速器，这里不展开说了。
-2. 第二种方法：使用 cloudflare 的优选 ip ，方法如下。（**不是加速器**，**不是加速器**，**不是加速器**）
+1. 第一种方法：使用**加速器**，这里不展开说了。
+2. 第二种方法：使用 cloudflare 的优选 ip ，查看[原理](#原理)。（**不是加速器**，**不是加速器**，**不是加速器**）